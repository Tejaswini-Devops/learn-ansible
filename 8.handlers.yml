- name: handlers demo
  hosts: localhost
  tasks:
    - name: copy sample
      ansible.builtin.template:
        src: teju.txt
        dest: /tmp/teju.txt

    - name: say hello again
      ansible.builtin.debug:
        msg: hello {{ name }} again.




